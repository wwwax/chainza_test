import { CustomButton, CustomImage } from './styles';

const SwapButton = () => {
  return (
    <CustomButton type="button">
      <CustomImage src="/downArrow.svg" alt="swap" />
    </CustomButton>
  );
};

export default SwapButton;
