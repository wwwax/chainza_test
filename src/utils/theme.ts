import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#051746',
    },
    success: {
      main: '#03a00f',
    },
    error: {
      main: 'rgba(255, 108, 108, 1)',
    },
    secondary: {
      main: '#FFFFFF',
    },
  },
});

export default theme;
